<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wygl.dao.NoticeDao">

    <resultMap id="resultMap" type="Notice">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="type" column="type"/>
        <result property="scope" column="scope"/>
        <result property="readCount" column="read_count"/>
        <result property="totalCount" column="total_count"/>
        <result property="publishDate" column="publish_date"/>
        <result property="status" column="status"/>
        <result property="operatorId" column="operator_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" resultMap="resultMap">
        SELECT * FROM t_notice WHERE id = #{id}
    </select>

    <select id="findPage" resultMap="resultMap">
        SELECT * FROM t_notice
        <where>
            <if test="queryString != null and queryString != ''">
                AND (title LIKE CONCAT('%', #{queryString}, '%')
                OR type LIKE CONCAT('%', #{queryString}, '%'))
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="type != null and type != ''">
                AND type = #{type}
            </if>
        </where>
        ORDER BY publish_date DESC
    </select>

    <insert id="insert" parameterType="Notice" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_notice (title, content, type, scope, read_count, total_count, publish_date, status, operator_id, create_time)
        VALUES (#{title}, #{content}, #{type}, #{scope}, #{readCount}, #{totalCount}, #{publishDate}, #{status}, #{operatorId}, NOW())
    </insert>

    <update id="update" parameterType="Notice">
        UPDATE t_notice
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="type != null">type = #{type},</if>
            <if test="scope != null">scope = #{scope},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM t_notice WHERE id = #{id}
    </delete>

</mapper>
