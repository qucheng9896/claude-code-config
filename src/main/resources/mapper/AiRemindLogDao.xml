<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wygl.dao.AiRemindLogDao">

    <resultMap id="aiRemindLogResultMap" type="AiRemindLog">
        <id property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="houseId" column="house_id"/>
        <result property="paymentIds" column="payment_ids"/>
        <result property="overdueAmount" column="overdue_amount"/>
        <result property="overdueDays" column="overdue_days"/>
        <result property="aiPrompt" column="ai_prompt"/>
        <result property="aiMessage" column="ai_message"/>
        <result property="sendChannel" column="send_channel"/>
        <result property="sendStatus" column="send_status"/>
        <result property="sendTime" column="send_time"/>
        <result property="operatorId" column="operator_id"/>
        <result property="createTime" column="create_time"/>
        <result property="tenantName" column="tenant_name"/>
        <result property="houseAddress" column="house_address"/>
    </resultMap>

    <select id="findPage" resultMap="aiRemindLogResultMap">
        SELECT l.*, t.name AS tenant_name,
               CONCAT(h.building_no, '-', h.room_no) AS house_address
        FROM t_ai_remind_log l
        LEFT JOIN t_tenant t ON l.tenant_id = t.id
        LEFT JOIN t_house h ON l.house_id = h.id
        <where>
            <if test="queryString != null and queryString != ''">
                AND (t.name LIKE CONCAT('%', #{queryString}, '%'))
            </if>
        </where>
        ORDER BY l.create_time DESC
    </select>

    <select id="findPending" resultMap="aiRemindLogResultMap">
        SELECT l.*, t.name AS tenant_name,
               CONCAT(h.building_no, '-', h.room_no) AS house_address
        FROM t_ai_remind_log l
        LEFT JOIN t_tenant t ON l.tenant_id = t.id
        LEFT JOIN t_house h ON l.house_id = h.id
        WHERE l.send_status = 0
        ORDER BY l.create_time DESC
    </select>

    <insert id="insert" parameterType="AiRemindLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_ai_remind_log (tenant_id, house_id, payment_ids, overdue_amount,
            overdue_days, ai_prompt, ai_message, send_channel, send_status, create_time)
        VALUES (#{tenantId}, #{houseId}, #{paymentIds}, #{overdueAmount},
            #{overdueDays}, #{aiPrompt}, #{aiMessage}, #{sendChannel}, #{sendStatus}, NOW())
    </insert>

    <update id="updateSendStatus">
        UPDATE t_ai_remind_log
        SET send_status = 1, send_time = NOW(), operator_id = #{operatorId}
        WHERE id = #{id}
    </update>

</mapper>
