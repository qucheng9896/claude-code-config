<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wygl.dao.EmployeeDao">

    <resultMap id="resultMap" type="Employee">
        <id property="id" column="id"/>
        <result property="empNo" column="emp_no"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="position" column="position"/>
        <result property="department" column="department"/>
        <result property="hireDate" column="hire_date"/>
        <result property="attendanceDays" column="attendance_days"/>
        <result property="orderCount" column="order_count"/>
        <result property="rating" column="rating"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findById" resultMap="resultMap">
        SELECT * FROM t_employee WHERE id = #{id}
    </select>

    <select id="findPage" resultMap="resultMap">
        SELECT * FROM t_employee
        <where>
            <if test="queryString != null and queryString != ''">
                AND (name LIKE CONCAT('%', #{queryString}, '%')
                OR emp_no LIKE CONCAT('%', #{queryString}, '%')
                OR phone LIKE CONCAT('%', #{queryString}, '%'))
            </if>
        </where>
        ORDER BY id
    </select>

    <insert id="insert" parameterType="Employee" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_employee (emp_no, name, phone, position, department, hire_date, attendance_days, order_count, rating, status, create_time)
        VALUES (#{empNo}, #{name}, #{phone}, #{position}, #{department}, #{hireDate}, #{attendanceDays}, #{orderCount}, #{rating}, #{status}, NOW())
    </insert>

    <update id="update" parameterType="Employee">
        UPDATE t_employee
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="position != null">position = #{position},</if>
            <if test="department != null">department = #{department},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM t_employee WHERE id = #{id}
    </delete>

</mapper>
