<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wygl.dao.UserDao">

    <resultMap id="userResultMap" type="User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="roleId" column="role_id"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="roleName" column="role_name"/>
    </resultMap>

    <select id="findByUsername" resultMap="userResultMap">
        SELECT u.*, r.role_name FROM t_user u
        LEFT JOIN t_role r ON u.role_id = r.id
        WHERE u.username = #{username}
    </select>

    <select id="findById" resultMap="userResultMap">
        SELECT u.*, r.role_name FROM t_user u
        LEFT JOIN t_role r ON u.role_id = r.id
        WHERE u.id = #{id}
    </select>

    <select id="findPage" resultMap="userResultMap">
        SELECT u.*, r.role_name FROM t_user u
        LEFT JOIN t_role r ON u.role_id = r.id
        <where>
            <if test="queryString != null and queryString != ''">
                AND (u.username LIKE CONCAT('%', #{queryString}, '%')
                OR u.name LIKE CONCAT('%', #{queryString}, '%'))
            </if>
            <if test="status != null">AND u.status = #{status}</if>
            <if test="type != null">AND u.role_id = #{type}</if>
        </where>
        ORDER BY u.create_time DESC
    </select>

    <insert id="insert" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, name, phone, email, role_id, status, create_time)
        VALUES (#{username}, #{password}, #{name}, #{phone}, #{email}, #{roleId}, #{status}, NOW())
    </insert>

    <update id="update" parameterType="User">
        UPDATE t_user
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="roleId != null">role_id = #{roleId},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updatePassword">
        UPDATE t_user SET password = #{password} WHERE id = #{id}
    </update>

    <update id="updateLastLoginTime">
        UPDATE t_user SET last_login_time = NOW() WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM t_user WHERE id = #{id}
    </delete>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM t_user WHERE username = #{username}
    </select>

</mapper>
